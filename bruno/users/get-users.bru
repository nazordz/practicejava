meta {
  name: List Users
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users
  body: none
  auth: none
}

headers {
  Accept: application/json
}

docs {
  List all users.

  GET /api/users — requires the `user:read` authority.
}
